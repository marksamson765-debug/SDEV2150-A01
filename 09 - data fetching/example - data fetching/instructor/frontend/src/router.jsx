// we'll just start off with some empty functions to bind to our react-router loaders
// (they're not doing anything yet)

export async function resourceDirectoryLoader() {}

export async function adminLoader() {}
