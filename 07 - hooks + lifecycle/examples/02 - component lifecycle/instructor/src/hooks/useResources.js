import { useEffect, useState } from 'react';


export function useResources() {
    // 1. I'm going to have state for: resources (holds fetched data), loading state, error msg

    // 2. I'm going to have an async function for fetching data from API (normally in its own layer)

    // 3. We're going to have an effect that fetches resources on page load

    // 4. We'll also have a refetch function
}